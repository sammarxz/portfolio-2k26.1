---
interface Props {
  as?: "p" | "h2";
  class?: string;
}

const { as: Tag = "p", class: className } = Astro.props;
---

<Tag
  class:list={[
    "text-xs font-mono uppercase tracking-widest text-neutral-500 mb-4",
    className,
  ]}
>
  <slot />
</Tag>
